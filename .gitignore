# Rust CMS Backend

# Build artifacts
target/

# Note: For applications it's recommended to commit Cargo.lock so CI and reproducible
# builds use the same resolved dependency graph. Do NOT ignore Cargo.lock here.

# Environment files (local secrets)
.env
.env.local

# Ignore other env variants but keep the example file
.env.*
!.env.example

# Sensitive or local-only directories
keys/
repo-mirror/
backups/

# Common secret/key file patterns
*.pem
*.key
*.crt
*.p12
*.pfx
*.jks
secrets/
*.secret
secrets.json
credentials.yml

# Cloud/local credentials
.aws/
.azure/

# Terraform
.terraform/
terraform.tfstate
terraform.tfstate.backup

# Trivy / scanner caches and local scan outputs
.trivyignore
.trivy/
trivy-report-*.json


# IDE settings
.vscode/
.idea/

# OS files
.DS_Store
Thumbs.db

# Uploads (keep a placeholder)
uploads/*
!uploads/.gitkeep

# Ignore platform-neutral path to instructions (use forward slashes)
.github/instructions/codacy.instructions.md

# Log files (temporary)
*.log
