# Doppler configuration file for Rust Backend
# プロジェクト設定 - メインCMSプロジェクトと統合
project: cms

# 環境設定
environments:
  development:
    config: dev
  staging:
    config: stg
  production:
    config: prd

# 設定マッピング
setup:
  project: cms
  config: dev

# Doppler CLI設定
cli:
  version: ">=3.0.0"
